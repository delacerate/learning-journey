const age = null;
// if we want to set a variable to empty we can use null so null is intentional
// let age;
// if we want to set a variable to undefined we can use undefined while undefined is unintentional
console.log(age, age + 3, `the age is ${age}`);// undefined
