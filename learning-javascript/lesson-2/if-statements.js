// if statements
const age = 25
if (age > 20) {
  console.log('you are over 20 years old')
}

const ninja = ['shaun', 'darman', 'ryu', 'yoshi']
if (ninja.length > 3) {
  console.log("that's alot of ninja")

}

const pass = 'password'

if (pass.length >= 8) {
  console.log('that password is long enough')
}
